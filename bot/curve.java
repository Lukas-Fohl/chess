package bot;

import framework.*;

public class curve{
    //2d array + curve with f(x)=2^(x)
    //origin 0;1
    //from current point
    //->next    x = x (current) + 1
    //          y = 2y(current) - 1 
    //->last    x = x (current) - 1
    //          y = (y(current)%2==0)?((y(current)-1)/2):((y(current))/2)
}